import React from "react";
import classes from "./SurgHistory.module.css";

function SurgHistory() {
  return (
    <div className={classes.mainContainer}>
      <p className={classes.heading}>SURGERY HISTORY</p>

      <div className={classes.infoContainer}>
        <div className={classes.gridContainer}>
          
        </div>
      </div>
    </div>
  );
}

export default SurgHistory;
